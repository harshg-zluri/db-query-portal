# API Configuration
# The base URL for the backend API
# In development, this proxies through Vite to the backend
# In production, set this to your actual backend URL (e.g., https://api.example.com)
VITE_API_BASE_URL=/api
